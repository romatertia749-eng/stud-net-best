# 🚀 Гайд по деплою на Koyeb

## 📋 Подготовка

### 1. Структура проекта
Убедись, что структура такая:
```
backend/
├── main.py          # Точка входа (FastAPI app)
├── config.py        # Конфигурация
├── requirements.txt # Зависимости
├── app/
│   ├── __init__.py
│   ├── database.py
│   ├── routers/
│   └── services/
└── uploads/         # Папка для загрузок
```

### 2. Проверь requirements.txt
Должен содержать:
```
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.23
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
Pillow>=10.2.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
```

## 🔧 Настройка в Koyeb

### Шаг 1: Создай новое приложение

1. Зайди на [koyeb.com](https://www.koyeb.com)
2. Нажми **"Create App"** или **"New App"**
3. Выбери **"GitHub"** (если код на GitHub) или **"Docker"**

### Шаг 2: Настрой Source (если через GitHub)

1. **Repository**: выбери свой репозиторий
2. **Branch**: `master` или `main`
3. **Build Command**: оставь пустым (Koyeb определит автоматически)
4. **Run Command**: **ВАЖНО!** Укажи:
   ```
   uvicorn main:app --host 0.0.0.0 --port $PORT
   ```
   Или если Koyeb использует другой порт:
   ```
   python -m uvicorn main:app --host 0.0.0.0 --port 8000
   ```

### Шаг 3: Настрой Build Settings

**Buildpack**: выбери `Python` или оставь автоматическое определение

**Root Directory**: если бэкенд в подпапке, укажи `backend/`

### Шаг 4: Environment Variables (КРИТИЧНО!)

Перейди в **Settings → Environment Variables** и добавь:

#### Обязательные переменные:

```bash
# База данных (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require

# JWT (можно использовать JWT_SECRET или JWT_SECRET_KEY)
JWT_SECRET=твой-секретный-ключ-минимум-32-символа
# ИЛИ
JWT_SECRET_KEY=твой-секретный-ключ-минимум-32-символа

# CORS - URL твоего фронтенда (Netlify/Vercel)
CORS_ORIGINS=https://твой-домен.netlify.app,https://твой-домен.vercel.app

# Порт (обычно Koyeb устанавливает автоматически через $PORT)
PORT=8000
```

#### Опциональные:

```bash
# Telegram Bot Token (если используешь)
TELEGRAM_BOT_TOKEN=твой-токен-бота

# Другие настройки (если нужно переопределить)
HOST=0.0.0.0
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=5
```

### Шаг 5: Настрой PostgreSQL Database

1. В Koyeb создай **PostgreSQL Database**:
   - Зайди в **Databases → Create Database**
   - Выбери **PostgreSQL**
   - Выбери регион (ближайший к твоему приложению)
   - Создай базу

2. Скопируй **Connection String** (DATABASE_URL):
   - Формат: `postgresql://user:password@host:port/dbname?sslmode=require`
   - Вставь в Environment Variables как `DATABASE_URL`

3. **ВАЖНО**: Если база создана отдельно, убедись что:
   - В Connection String есть `?sslmode=require` в конце
   - Пользователь имеет права на создание таблиц

## 🐛 Частые проблемы и решения

### Проблема 1: "Module not found" или импорты не работают

**Решение:**
- Убедись что `Root Directory` в Koyeb = `backend/`
- Или перемести все файлы в корень репозитория
- Проверь что `main.py` находится в корне папки, которую указываешь

### Проблема 2: "Port already in use" или приложение не запускается

**Решение:**
- В Run Command используй: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- Koyeb автоматически устанавливает `$PORT` через переменную окружения
- Если не работает, попробуй: `python -m uvicorn main:app --host 0.0.0.0 --port 8000`

### Проблема 3: База данных не подключается

**Решение:**
- Проверь что `DATABASE_URL` правильный (скопирован из Koyeb Database)
- Убедись что в конце есть `?sslmode=require`
- Проверь что база данных и приложение в одном регионе
- Проверь логи: `koyeb logs <app-name>`

### Проблема 4: CORS ошибки

**Решение:**
- Убедись что `CORS_ORIGINS` содержит точный URL фронтенда
- Формат: `https://domain.com` (без слеша в конце)
- Если несколько доменов: `https://domain1.com,https://domain2.com`
- Проверь что нет пробелов после запятых

### Проблема 5: JWT ошибки или "Invalid token"

**Решение:**
- Убедись что `JWT_SECRET` или `JWT_SECRET_KEY` установлен
- Ключ должен быть минимум 32 символа
- Используй безопасный случайный ключ (можно сгенерировать: `openssl rand -hex 32`)

### Проблема 6: Файлы не загружаются (uploads)

**Решение:**
- Папка `uploads/` должна существовать
- Koyeb использует ephemeral storage (временное хранилище)
- Файлы удаляются при перезапуске
- Для постоянного хранения используй S3 или другой storage

### Проблема 7: Приложение падает с ошибкой валидации

**Решение:**
- Проверь логи: `koyeb logs <app-name>`
- Убедись что все переменные окружения установлены
- Проверь что код обновлён (последний коммит задеплоен)

## 📝 Проверка деплоя

### 1. Проверь логи

В Koyeb Dashboard:
- Зайди в **Apps → [твоё приложение] → Logs**
- Ищи ошибки импорта, подключения к БД, и т.д.

### 2. Проверь health endpoint

Открой в браузере:
```
https://твой-домен.koyeb.app/health
```

Должен вернуть:
```json
{"status": "ok", "service": "StudNet API"}
```

### 3. Проверь API endpoints

```
https://твой-домен.koyeb.app/api
https://твой-домен.koyeb.app/routes
```

### 4. Проверь подключение к БД

В логах должно быть:
```
✅ DATABASE_URL загружен из переменных окружения
✅ JWT_SECRET_KEY загружен из переменных окружения
✅ CORS_ORIGINS загружен: https://твой-домен.netlify.app
```

## 🔍 Отладка через CLI

Установи Koyeb CLI:
```bash
# macOS/Linux
curl https://www.koyeb.com/cli.sh | sh

# Windows (через Scoop)
scoop install koyeb
```

Команды:
```bash
# Логин
koyeb auth login

# Просмотр логов
koyeb logs <app-name>

# Список приложений
koyeb apps list

# Переменные окружения
koyeb services envs list <service-name>
```

## ✅ Чеклист перед деплоем

- [ ] `requirements.txt` содержит все зависимости
- [ ] `main.py` находится в правильной папке
- [ ] `DATABASE_URL` установлен и правильный
- [ ] `JWT_SECRET` или `JWT_SECRET_KEY` установлен
- [ ] `CORS_ORIGINS` содержит URL фронтенда
- [ ] Run Command правильный: `uvicorn main:app --host 0.0.0.0 --port $PORT`
- [ ] Root Directory указан правильно (если бэкенд в подпапке)
- [ ] Код закоммичен и запушен в репозиторий
- [ ] База данных создана и доступна

## 🎯 Пример правильной конфигурации

### Environment Variables:
```
DATABASE_URL=postgresql://user:pass@host:5432/db?sslmode=require
JWT_SECRET=super-secret-key-minimum-32-characters-long-random-string
CORS_ORIGINS=https://studnet-app.netlify.app
PORT=8000
```

### Run Command:
```
uvicorn main:app --host 0.0.0.0 --port $PORT
```

### Build Command:
(оставить пустым или автоматически)

### Root Directory:
```
backend/
```
(если бэкенд в подпапке `backend/`)

## 🆘 Если ничего не помогает

1. **Проверь логи полностью** - там должна быть точная ошибка
2. **Убедись что локально всё работает** - запусти `python run.py` локально
3. **Проверь версию Python** - Koyeb должен использовать Python 3.11+
4. **Создай новый деплой** - иногда помогает пересоздать приложение с нуля
5. **Проверь документацию Koyeb**: https://www.koyeb.com/docs

## 📞 Полезные ссылки

- Koyeb Dashboard: https://app.koyeb.com
- Koyeb Docs: https://www.koyeb.com/docs
- FastAPI Docs: https://fastapi.tiangolo.com



