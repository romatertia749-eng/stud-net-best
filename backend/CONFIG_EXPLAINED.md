# Зачем нужен config.py и где хранить секреты

## 🎯 Назначение config.py

`config.py` **НЕ хранит секреты**. Он только **читает** переменные окружения.

### Как это работает:

1. **Значения в коде** (строки 15-34) - это **значения по умолчанию** (fallback)
   - Используются только если переменная окружения не найдена
   - Для разработки на локальной машине
   - **НЕ для продакшена!**

2. **BaseSettings** автоматически ищет переменные окружения:
   - Сначала проверяет переменные окружения системы
   - Если не найдено - использует значение по умолчанию из кода
   - Также может читать из файла `.env` (для локальной разработки)

## ✅ Правильный способ (для Koyeb):

### В Koyeb (Settings → Environment Variables):

```
DATABASE_URL=postgresql://user:password@host:port/db?sslmode=require
JWT_SECRET_KEY=ваш-секретный-ключ-здесь
CORS_ORIGINS=https://ваш-сайт.netlify.app
TELEGRAM_BOT_TOKEN=ваш-токен-бота
```

### В config.py:

```python
DATABASE_URL: str = "postgresql://user:password@localhost:5432/networking_app"
# ↑ Это значение по умолчанию, используется ТОЛЬКО если переменная окружения не найдена
```

**Приоритет загрузки:**
1. Переменная окружения из Koyeb ← **Используется в продакшене**
2. Файл `.env` (если есть) ← Для локальной разработки
3. Значение по умолчанию в коде ← Только если ничего не найдено

## ❌ НЕПРАВИЛЬНО (не делай так):

```python
# НЕПРАВИЛЬНО - не пиши секреты в код!
DATABASE_URL: str = "postgresql://real-user:real-password@real-host:5432/real-db"
JWT_SECRET_KEY: str = "мой-реальный-секретный-ключ"
```

**Почему плохо:**
- Код попадает в Git → секреты в репозитории
- Любой, кто видит код, видит секреты
- Невозможно использовать разные настройки для разных окружений

## 📋 Что должно быть где:

### В config.py (код):
- ✅ Типы данных
- ✅ Значения по умолчанию для разработки
- ✅ Валидация (например, парсинг CORS_ORIGINS)

### В Koyeb (Environment Variables):
- ✅ DATABASE_URL (строка подключения к Neon)
- ✅ JWT_SECRET_KEY (секретный ключ)
- ✅ CORS_ORIGINS (URL вашего Netlify сайта)
- ✅ TELEGRAM_BOT_TOKEN (если нужен)

### В .env (только для локальной разработки):
- ✅ Те же переменные, что и в Koyeb
- ⚠️ Файл `.env` должен быть в `.gitignore` (не попадает в Git)

## 🔍 Как проверить, что переменные загружаются:

1. **В логах Koyeb** при старте приложения должно быть видно, какие переменные загружены
2. **Через API**: `GET /api/config/check` (показывает статус без секретов)

## 💡 Итог:

**config.py = инструкция "какие переменные нужны и какие у них типы"**
**Koyeb Environment Variables = реальные секретные данные**

Не нужно менять значения в config.py для продакшена - они автоматически подхватятся из переменных окружения Koyeb!
