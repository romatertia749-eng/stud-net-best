# 🚀 Деплой на Vercel

## 📋 Быстрая настройка

### 1. Подготовка репозитория

Убедись, что все изменения закоммичены и запушены в GitHub:
```bash
git add .
git commit -m "Add Vercel configuration"
git push
```

### 2. Создание проекта в Vercel

1. Зайди на [vercel.com](https://vercel.com)
2. Нажми **"Sign Up"** или **"Log In"** (можно через GitHub)
3. После входа нажми **"Add New..."** → **"Project"**
4. Выбери свой GitHub репозиторий `stud-net`
5. Vercel автоматически определит настройки из `vercel.json`

### 3. Настройка проекта

Vercel автоматически определит:
- **Framework Preset**: Vite
- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

**Важно**: Оставь все настройки по умолчанию, они уже настроены в `vercel.json`

### 4. Environment Variables (опционально)

Если нужно переопределить API URL, добавь в Vercel:
- **Settings → Environment Variables**
- Добавь: `VITE_API_BASE_URL` = `https://твой-домен.koyeb.app`

Если не добавишь, будет использован дефолтный URL из `src/config/api.js`

### 5. Деплой

1. Нажми **"Deploy"**
2. Дождись завершения сборки (обычно 1-2 минуты)
3. Vercel даст тебе URL типа: `stud-net-xxx.vercel.app`

### 6. Обновление CORS в Koyeb

После получения URL от Vercel:

1. Зайди в Koyeb Dashboard
2. Перейди в **Settings → Environment Variables**
3. Обнови `CORS_ORIGINS` (включи Telegram домены для Mini Apps):
   ```
   http://localhost:5173,https://stud-net-best.vercel.app,https://web.telegram.org,https://webk.telegram.org,https://webz.telegram.org
   ```
   (замени `stud-net-best.vercel.app` на твой реальный домен Vercel, если отличается)

4. Перезапусти приложение в Koyeb (или подожди автоматического перезапуска)

**Важно**: Telegram домены (`web.telegram.org`, `webk.telegram.org`, `webz.telegram.org`) необходимы для работы Mini Apps в Telegram

### 7. Настройка кастомного домена (опционально)

1. В Vercel Dashboard → **Settings → Domains**
2. Добавь свой домен
3. Следуй инструкциям для настройки DNS

## ✅ Проверка деплоя

1. Открой URL от Vercel в браузере
2. Проверь, что приложение загружается
3. Проверь авторизацию через Telegram
4. Проверь работу API (должен подключаться к Koyeb бэкенду)

## 🔄 Автоматический деплой

Vercel автоматически деплоит:
- При каждом push в `main`/`master` ветку → Production
- При создании Pull Request → Preview деплой

## 📝 Структура конфигурации

Файл `vercel.json` содержит:
- `buildCommand`: команда сборки
- `outputDirectory`: папка с собранными файлами
- `rewrites`: правила для SPA (все запросы → index.html)
- `headers`: кеширование статических файлов

## 🐛 Решение проблем

### Проблема: Сайт не загружается
- Проверь логи в Vercel Dashboard → **Deployments**
- Убедись, что сборка прошла успешно

### Проблема: CORS ошибки
- Проверь, что обновил `CORS_ORIGINS` в Koyeb
- Убедись, что URL в Koyeb точно совпадает с URL Vercel (с https://)

### Проблема: API не работает
- Проверь, что бэкенд на Koyeb запущен
- Проверь URL API в `src/config/api.js`
- Проверь логи в Koyeb Dashboard

## 🎯 Преимущества Vercel

- ✅ Бесплатный план с хорошими лимитами
- ✅ Автоматический деплой из GitHub
- ✅ Preview деплои для PR
- ✅ Быстрая CDN
- ✅ Простая настройка
- ✅ SSL сертификаты автоматически

## 📞 Полезные ссылки

- Vercel Dashboard: https://vercel.com/dashboard
- Vercel Docs: https://vercel.com/docs
- Vite + Vercel: https://vercel.com/docs/frameworks/vite
