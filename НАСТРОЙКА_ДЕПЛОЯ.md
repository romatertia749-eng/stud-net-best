# Настройка деплоя StudNet

## ⚠️ ВАЖНО: Что нужно сделать перед использованием

### 1. Применить схему базы данных в Neon

**Это обязательно!** Без этого создание профилей может не работать правильно.

1. Откройте ваш проект в Neon Console
2. Перейдите в SQL Editor
3. Откройте файл `schema.sql` из корня проекта
4. Скопируйте весь его содержимое
5. Вставьте в SQL Editor в Neon
6. Нажмите "Run" или выполните запрос

Это создаст:
- Все таблицы (profiles, swipes, matches)
- Индексы для быстрых запросов
- Триггеры для автоматического создания мэтчей
- Функции для поиска профилей

### 2. Настроить переменные окружения в Koyeb

В настройках вашего приложения в Koyeb (Settings → Environment Variables) добавьте:

```
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
```
**Где взять:** Neon → ваш проект → Connection Details → Connection string

```
CORS_ORIGINS=https://ваш-сайт.netlify.app
```
**Где взять:** URL вашего сайта на Netlify

```
JWT_SECRET_KEY=сгенерируйте-случайный-ключ
```
**Как сгенерировать:** `openssl rand -hex 32` (или любой генератор случайных строк)

### 3. Настроить переменные окружения в Netlify

В настройках Netlify (Site settings → Environment variables) добавьте:

```
VITE_API_BASE_URL=https://ваш-бэкенд.koyeb.app
```
**Где взять:** URL вашего бэкенда в Koyeb

### 4. Проверить работу

1. Откройте: `https://ваш-бэкенд.koyeb.app/`
   - Должно показать: `{"message": "StudNet API", "version": "1.0.0"}`

2. Откройте: `https://ваш-бэкенд.koyeb.app/docs`
   - Должна открыться документация API (Swagger UI)

3. Попробуйте создать профиль в приложении
   - Если ошибка - проверьте логи в Koyeb и консоль браузера

## ❌ Частые проблемы

### "Failed to fetch" при создании профиля
- Проверьте `CORS_ORIGINS` в Koyeb (должен включать Netlify URL)
- Проверьте `VITE_API_BASE_URL` в Netlify

### "Database connection error"
- Проверьте `DATABASE_URL` в Koyeb
- Убедитесь, что база данных Neon активна

### Профили не создаются
- **Самая частая причина:** не применён `schema.sql` в Neon
- Проверьте логи Koyeb на ошибки SQL

### Фотографии пропадают
- Это нормально для Koyeb (файлы хранятся временно)
- Для продакшена нужен S3/Cloudinary

## ✅ После настройки

Если всё настроено правильно:
- ✅ Создание профилей работает
- ✅ Просмотр профилей работает  
- ✅ Лайки/пропуски работают
- ✅ Мэтчи создаются автоматически

## 📝 Примечание

Код в `main.py` создаёт базовые таблицы автоматически, но для полной функциональности (индексы, триггеры, функции) нужно применить `schema.sql` вручную в Neon.
