# Чеклист для деплоя StudNet

## ✅ Что нужно проверить перед использованием

### 1. База данных (Neon)

**Обязательно выполните `schema.sql` в базе данных Neon:**
- Откройте Neon Console
- Подключитесь к вашей базе данных
- Выполните весь файл `schema.sql` (копируйте и вставьте в SQL редактор)
- Это создаст все таблицы, индексы, триггеры, функции и представления

**Почему это важно:**
- `Base.metadata.create_all()` в коде создаёт только базовые таблицы
- Без индексов запросы будут медленными
- Без триггеров мэтчи не будут создаваться автоматически
- Без функций некоторые запросы не будут работать

### 2. Переменные окружения в Koyeb

В настройках вашего приложения в Koyeb установите:

```env
DATABASE_URL=postgresql://user:password@host:port/database
# URL из Neon (Connection String)

CORS_ORIGINS=https://your-app.netlify.app,https://your-app.netlify.app/*
# Замените на ваш реальный Netlify URL

JWT_SECRET_KEY=your-very-secret-key-here
# Сгенерируйте: openssl rand -hex 32

JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

TELEGRAM_BOT_TOKEN=your-bot-token
# Опционально, если хотите проверять Telegram данные

UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=5
```

**Как найти DATABASE_URL в Neon:**
1. Откройте ваш проект в Neon
2. Перейдите в "Connection Details"
3. Скопируйте "Connection string" (выберите вариант с паролем)
4. Формат: `postgresql://user:password@host:port/database?sslmode=require`

### 3. CORS настройки

Убедитесь, что `CORS_ORIGINS` включает ваш Netlify URL:
- Если ваш сайт: `https://myapp.netlify.app`
- Добавьте: `https://myapp.netlify.app` в `CORS_ORIGINS`

### 4. Переменные окружения в Netlify

В настройках Netlify (Site settings → Environment variables) установите:

```env
VITE_API_BASE_URL=https://your-koyeb-app.koyeb.app
# Замените на ваш реальный Koyeb URL
```

### 5. Проверка работы

После деплоя проверьте:

1. **Бэкенд доступен:**
   ```
   https://your-koyeb-app.koyeb.app/
   ```
   Должен вернуть: `{"message": "StudNet API", "version": "1.0.0"}`

2. **API документация:**
   ```
   https://your-koyeb-app.koyeb.app/docs
   ```
   Должна открыться Swagger UI

3. **Создание профиля:**
   - Откройте приложение в Telegram
   - Попробуйте создать профиль
   - Проверьте консоль браузера на ошибки

### 6. Проблемы с загрузкой файлов

⚠️ **Важно:** В Koyeb файловая система эфемерная. Загруженные файлы будут теряться при перезапуске контейнера.

**Временное решение:** Файлы будут работать, но могут пропадать.

**Постоянное решение:** Используйте облачное хранилище:
- AWS S3
- Cloudinary
- DigitalOcean Spaces
- Или другой S3-совместимый сервис

### 7. Логи и отладка

Если что-то не работает:

1. **Проверьте логи Koyeb:**
   - Откройте ваш сервис в Koyeb
   - Перейдите в "Logs"
   - Ищите ошибки подключения к БД или другие проблемы

2. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Смотрите вкладку "Console" и "Network"
   - Проверьте, какие запросы падают

3. **Проверьте подключение к БД:**
   - В Neon Console выполните: `SELECT COUNT(*) FROM profiles;`
   - Если ошибка - схема не применена

## 🚨 Частые проблемы

### Ошибка: "Failed to fetch"
- Проверьте `CORS_ORIGINS` в Koyeb
- Проверьте, что бэкенд доступен по URL
- Проверьте, что `VITE_API_BASE_URL` правильный в Netlify

### Ошибка: "Database connection error"
- Проверьте `DATABASE_URL` в Koyeb
- Убедитесь, что база данных Neon активна
- Проверьте, что в URL правильный пароль

### Ошибка: "Profile not found" при создании
- Проверьте, что `schema.sql` применён к базе данных
- Проверьте логи Koyeb на ошибки SQL

### Фотографии не загружаются
- Это нормально для Koyeb (эфемерная файловая система)
- Нужно настроить облачное хранилище для продакшена

## ✅ После настройки

Если всё настроено правильно, вы сможете:
- ✅ Создавать профили
- ✅ Просматривать профили
- ✅ Делать лайки/пропуски
- ✅ Видеть входящие лайки
- ✅ Создавать мэтчи
